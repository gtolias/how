# Demo parameters for training of HOW method R18

__template__: _train_how.yml

training:
  optimizer:
    lr: 5.e-6 # Adam learning rate

model:
  architecture: resnet18 # Backbone network
  skip_layer: 0 # How many layers of blocks will be skipped, counting from end
